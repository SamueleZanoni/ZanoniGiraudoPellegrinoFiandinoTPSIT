<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <style>
        /* Sfondo nero */
        body {
            background-color: black;
            color: white; /* Testo bianco per contrasto */
            font-family: Arial, sans-serif; /* Font più leggibile */
        }

        .container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin: 20px;
            align-items: center; /* Centra tutto orizzontalmente */
        }

        .row {
            display: flex;
            align-items: center; /* Allinea verticalmente il titolo e l'immagine */
            gap: 20px; /* Distanza tra titolo e immagine */
            width: 80%; /* Larghezza massima */
        }

        .title {
            font-size: 20px;
            font-weight: bold;
            width: 200px; /* Larghezza fissa per i titoli */
            text-align: left; /* Allinea i titoli a sinistra */
            color: white; /* Testo bianco */
        }

        .image img {
            width: 500px;
            height: auto;
            display: block;
            filter: contrast(1.2) brightness(1.1) saturate(1.3); /* Aumenta contrasto, luminosità e saturazione */
            transition: transform 0.3s ease-in-out; /* Effetto zoom */
        }

            /* Effetto di ingrandimento al passaggio del mouse */
            .image img:hover {
                transform: scale(1.05); /* Ingrandisce leggermente */
            }
    </style>
</head>
<body>
    <h1 style="text-align: center;">Mappe</h1>

    <div class="container">
        <div class="row">
            <div class="title">Nuvole</div>
            <div class="image"><img src="@ViewData["MappaNuvole"]" alt="Mappa Nuvole" /></div>
        </div>
        <div class="row">
            <div class="title">Pressione</div>
            <div class="image"><img src="@ViewData["MappaPressione"]" alt="Mappa Pressione" /></div>
        </div>
        <div class="row">
            <div class="title">Vento</div>
            <div class="image"><img src="@ViewData["MappaVento"]" alt="Mappa Vento" /></div>
        </div>
        <div class="row">
            <div class="title">Temperatura</div>
            <div class="image"><img src="@ViewData["MappaTemperatura"]" alt="Mappa Temperatura" /></div>
        </div>
        <div class="row">
            <div class="title">Precipitazioni</div>
            <div class="image"><img src="@ViewData["MappaPrecipitazioni"]" alt="Mappa Precipitazioni" /></div>
        </div>
    </div>
</body>
</html>
